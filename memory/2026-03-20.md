
## Daily Memory Flush: 2026-03-20

### Key Events & Status
- **Automation Review**: Redpacket monitor (Scrapling) initially hit 401s, successfully reverted to stable `requests` implementation.
- **Social Automation**: Deployed `moltbook_agent_pro.py` (Background Daemon). Includes math-challenge solver for verification.
- **Notification Failure**: SMTP channel (QQ Mail) blocked automated raw content (spam detection). Switched to manual curation + refined automated formatting.
- **Infrastructure Issue**: Feishu API 400 error. App configuration or token expiry needs manual reset in Developer Console.
- **Maintenance**: Automated "news_fetcher" Crontab was missing; restored and verified.

### Status of Agents
- **Moltbook Agent Pro**: Running (PID 1094).
- **Redpacket Monitor**: Running (PID 1093).
- **News Cron**: 07:30 daily (Restored).

### Lessons Learned
- **Deliverability**: Never send raw API output via email; always filter, clean, and format.
- **Persistence**: Crontab registration must be idempotent; system reboots wipe temporary cron modifications.
- **Fail-Fast**: Stop using unverified scrape logic (Scrapling) if it interferes with auth headers; favor stability.

### Open TODOs
- [ ] Refresh Feishu App Credentials (App ID/Secret).
- [ ] Monitor email deliverability of the new "Clean Format".
--- END OF FLUSH ---

## 2026-03-20 14:00 - Model Configuration Tuning
- User discovered incorrect model setting for `qwen/qwen3-coder:free` causing API errors.
- Files `models.json` and `auth-profiles.json` under `/root/.openclaw/agents/main/agent/` confirmed as the source.
- User will manually edit these JSON files; advised to restart gateway for changes to take effect.

## 2026-03-20 14:10 - Redpacket Sniper Redeployment
- **Target**: 二号小龙虾 (ID: `ba239429-7544-4d52-8c85-da7ae6ea1672`)
- **Action**: Followed the target via API. Found that weekend red packets were already claimed/completed.
- **Automation**: Deployed new stable and sniper python scripts (`redpacket_daemon_v2.py` and `redpacket_monitor_scrapling.py`).
- **Configuration**:
  - `redpacket_daemon_v2.py`: Global + specific creator polling (interval: 15s). (Later killed).
  - `redpacket_monitor_scrapling.py`: Specifically polls "二号小龙虾" every 60s. Auto claims, auto generates payment link, and posts a "thanks" Moment on success. Currently running as a daemon (PID 25426).
- **Result**: High-speed snipers are armed and monitoring target drops quietly.
