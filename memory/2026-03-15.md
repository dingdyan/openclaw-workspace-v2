# 2026-03-15 记忆日志

## 系统重启与记忆服务迁移

### OpenViking 记忆服务部署（核心基础设施升级）
- **问题**：EvoMap Embedding 配额耗尽（429 insufficient_quota），`memory_search` 原生 API 失效
- **解决方案**：迁移至本地 OpenViking 向量数据库服务
- **API Keys 配置**（Volcengine/Ark）：
  - Embedding Dense API Key: `dc06e7d2-173a-4ec9-ab20-b59c1df5c812`
  - VLM API Key: `55545008-8fdc-4e7b-8bf4-699e77581386`
- **配置文件**：`/root/code/openviking/examples/mcp-query/ov.conf`
  - Embedding Model: `doubao-embedding-vision-250615` (1024 维度)
  - VLM Model: `doubao-seed-2-0-pro-260215`
  - Provider: Volcengine (火山引擎)
- **服务端口**：2033 (localhost)
- **守护进程**：已配置 `while true` 自恢复脚本，服务崩溃后 1 秒内自动重启
- **使用方式**：
  - 原生 `memory_search` 按钮已弃用（仍指向 OpenAI/EvoMap）
  - 改用 OpenViking MCP 接口直接调用（`query` / `search` 工具）
  - 通过 `mcporter` 或直接 HTTP POST 到 `http://127.0.0.1:2033/mcp`

### 子代理网络状态
- **四个牛马子代理**全部正常运行：
  - `subagent-monad-bot`：Monad Testnet 日常交互
  - `subagent-moltbook-miner`：Moltbook $ALPHA Mint 及营销
  - `subagent-tilt-monitor`：Tilt Protocol XAF 基金监控
  - `subagent-redpacket-bot`：Clawpi 红包监听（Lobster Pie 活动）

### 模型配置
- **当前模型**：Gemini 3.1 Flash Lite（qwen-portal/coder-model 为默认别名）
- **策略**：高频交互使用 Flash 节省额度，深度决策时切换 Pro

### 关键决策
- **记忆检索策略**：绕过失效的原生 `memory_search` API，直接通过 OpenViking MCP 接口进行语义检索
- **服务稳定性**：OpenViking 服务已挂载自恢复守护进程，确保永久在线

---
*本日志由 AI CEO 小安自动生成，记录系统关键变更与链上任务状态*

## 用户邮箱配置（永久记忆）
- **用户邮箱**：`5581223@qq.com`（老板的默认收件邮箱）
- **配置时间**：2026-03-15 12:39 UTC
- **用途**：所有系统通知、重要邮件默认发送至此地址
- **SendClaw 代理邮箱**：`xiaoan_ceo@sendclaw.com`（用于自主收发）
